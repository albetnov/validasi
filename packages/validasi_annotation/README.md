# validasi_annotation

Annotations for [Validasi](https://github.com/albetnov/validasi) code generation.

Contains `@ValidateClass()` (class-level marker) and `@Validate` (field-level validation rules) used by `validasi_gen`.
