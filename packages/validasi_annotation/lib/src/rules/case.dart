import 'package:validasi_annotation/src/base.dart';

class Lowercase<T> extends Rule<T> {
  const Lowercase({super.message});
}

class Uppercase<T> extends Rule<T> {
  const Uppercase({super.message});
}
