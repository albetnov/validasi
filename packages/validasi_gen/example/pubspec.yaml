name: validasi_gen_example
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  validasi:
    path: ../../validasi

dev_dependencies:
  build_runner: ^2.4.12
  test: ^1.24.0
  validasi_gen:
    path: ../
