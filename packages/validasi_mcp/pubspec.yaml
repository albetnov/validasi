name: validasi_mcp
description: MCP server providing Validasi documentation and API reference for AI assistants.
version: 0.1.0-beta.4
repository: https://github.com/albetnov/validasi

environment:
  sdk: ^3.5.0
resolution: workspace

dependencies:
  dart_mcp: ^0.5.0
  stream_channel: ^2.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
