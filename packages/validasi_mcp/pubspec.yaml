name: validasi_mcp
description: MCP stdio adapter for Validasi schemas and validation tools.
version: 0.1.0-beta.2
repository: https://github.com/albetnov/validasi

environment:
  sdk: ^3.5.0
resolution: workspace

dependencies:
  dart_mcp: ^0.5.0
  stream_channel: ^2.1.4
  validasi: ^1.0.0-dev.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
